/*
 * args.c
 *
 *  Created on: 2018-01-13
 *      Author: malego
 */


#include "args.h"



int parse_args(int argc, char **argv)
{
/*	char str[128];
	char filename[128];
	char filename_out[128];
	int fileDump;

	    if (argc > 1) {
		printf("Starting with private key %s\n", argv[1]);
		//strcpy(start_privkey_hex, argv[1]);
        int pos = 0;
        const char* ch = argv[1];
        while (pos < 32) {
            unsigned short sh;
            if (sscanf(ch, "%2hx", &sh))
			{
                str[pos] = sh;
            }
			else
			{
				printf("Invalid PrivateKey Provided\n");
				return 0;
            }
            ch += 2;
            pos++;
		}
    }

	if (argc > 2) {
		sprintf(filename, "%sKeys_PubC20PubU20Priv32_%s.txt", argv[2], start_privkey_hex);
	}else{
		sprintf(filename, "/media/malego/Data2TB/Keys_PubC20PubU20Priv32_%s.txt", start_privkey_hex);
	}

	sprintf(filename_out, "/media/malego/Data2TB/PubkeyHash160_%s.bin", "crackstation");
	fileDump = open_outfile(filename_out);
*/
	return 0;
}
